query searchICDGroup($pagination: Pagination,$query: PaginationDefaultQueryInput!) {
    gIcdGroups(query: $query, pagination: $pagination) {
        hasMore
        items {
            ...ICDGroupItem
        }
    }
}