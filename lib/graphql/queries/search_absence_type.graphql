query searchAbsenceTypes($pagination: Pagination,$query: PaginationDefaultQueryInput!) {
    absenceTypes(query: $query, pagination: $pagination) {
        hasMore
        items {
            ...AbsenceTypeItem
        }
    }
}